type Orders @model @auth(rules: [{allow: public}]) {
  id: ID!
  OrderId: String
  CustomerId: String
  StoreId: String
  OrderStatus: String
  Order: AWSJSON
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}
 